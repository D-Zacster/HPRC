# To create documentation for functions based on comments in their code run:
# library(devtools)
# setwd(r"(C:\Users\Zacb3\OneDrive - University of Oklahoma\HPRC Files\R\HPRC)")
# document()

# install("./HPRC") while in the project will install what is currently written

# devtools::install_github('D-Zacster/HPRC') will install from github

# The following lines in git will add changes, commit them, select the desired
# branch (optional), then push the commit.
# git add .
# git commit -m "Some commit message"
# git branch -M main
# git push -u origin main
